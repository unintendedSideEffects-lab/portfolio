/* ================================================================
   RACHEL BUILDS — Shared JavaScript
   Common utilities loaded across all pages.
   Page-specific scripts remain in their respective HTML files.
================================================================ */

'use strict';

/* ── Future shared utilities can go here ──────────────────────
   Currently each page has its own unique scripts (typing animation,
   generative art, password gate, pixel-rabbit animations, etc.).
   This file exists so shared behaviour (e.g., nav interactions,
   analytics hooks, dark-mode toggle) has a single home when needed.
──────────────────────────────────────────────────────────────── */
